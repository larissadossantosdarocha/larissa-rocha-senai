#include <stdio.h>

int main(){
    int numero, a;
    
    printf("digite um numero para ver a tabuada: ");
    scanf("%d", &numero);
    
    a=1;
    while(a <=10 ) {
    printf("%d*%d = %d\n", numero ,a,  (numero*a)); 
    a++;
}  

 return 0;
 
 }
